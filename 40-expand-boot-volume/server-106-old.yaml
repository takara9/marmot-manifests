apiVersion: v1
kind: Server
metadata:
    name: server-106x
    comment: ブートボリュームを拡張するサーバー
spec:
    cpu: 1
    memory: 1024
    mmImage: ubuntu24.04
    bootVolume:
        spec:
            size: 50
            type: qcow2
    auth:
        url: https://github.com/takara9.keys
    networkInterface:
        - networkname: host-bridge
