## rootパスワードの変更

root のパスワードを変更できます。

この機能の使用は非推奨ですが、機能が無くなるわけではありません。
marmot は、セキュリティが確保されたローカルの実験用のため、この機能を残しておきます。

ただし、クリアテキストでJSON形式でネットワークを流れ、
かつ、etcd に暗号化されずに保存されるため、セキュリティ上改善が必要です。

```yaml
spec:
    auth:
        rootPassword: password123
```

これにより、root で設定したパスワードでログインできます。
