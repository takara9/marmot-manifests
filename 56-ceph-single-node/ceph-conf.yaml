# Ceph-CSI プラグインが参照する ceph.conf 設定
apiVersion: v1
kind: ConfigMap
data:
  ceph.conf: |
    [global]
    auth_cluster_required = cephx
    auth_service_required = cephx
    auth_client_required = cephx
  # keyring は必須キーだが、値は空のままでよい
  keyring: |
metadata:
  name: ceph-config
