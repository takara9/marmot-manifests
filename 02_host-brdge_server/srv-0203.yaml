apiVersion: v1
kind: Server
metadata:
    name: srv-0203
    comment: ブリッジ接続する仮想サーバー、IPアドレス自動割り当て、公開鍵利用
spec:
    cpu: 1
    memory: 1024
    mmImage: ubuntu24.04
    auth:
        url: https://github.com/takara9.keys
    networkInterface:
        - networkname: host-bridge
