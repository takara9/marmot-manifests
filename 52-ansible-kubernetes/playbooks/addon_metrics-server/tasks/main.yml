- name: Install Metrics Server
  get_url:
    url: "https://github.com/kubernetes-sigs/metrics-server/releases/download/{{ k8s_metrics_server }}/components.yaml"
    dest: "{{ temp_dir }}/metrics-server.yaml"
    owner: "{{ cluster_admin }}"
    group: "{{ cluster_admin }}"
    mode: '0644'

- name: add --kubelet-insecure-tls to metrics-server
  blockinfile:
    path: "{{ temp_dir }}/metrics-server.yaml"
    insertafter: '        - --metric-resolution=15s'
    block: |
           # added lines
                   - --kubelet-insecure-tls

- name: Deploy Metrics Server
  command: kubectl apply -f {{ temp_dir }}/metrics-server.yaml
  become_user: "{{ cluster_admin }}"
  args:
    chdir: "{{ temp_dir }}"
