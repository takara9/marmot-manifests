apiVersion: v1
kind: Server
metadata:
    name: test-1
    comment: Python REST APIサーバー test-1
spec:
    cpu: 1
    memory: 2048
    mmImage: ubuntu24.04
    auth:
        url: https://github.com/takara9.keys
    ansible:
        playbook: playbook/rest-svc-1-setup.yaml
        inventory: playbook/hosts.yaml
    networkInterface:
        - networkname: host-bridge
        - networkname: db-net
        - networkname: rest-net
